#Interview Questions:-
#why is a switch more secutre than hub?
Switch directs the data to opnly particular intended host address but Hub sends the data to all connected devices, that's makes it more malicious comap;red to Switch.
#why does google uses DNS?
Computers only undersatnds binary bits, so when a user requests for data in any human understabale language, DNS converts it into Binary languange and sends the response.
#what happens if DNS stops working?
If DNS stops working user will not requested data, no web page address will respond.
#why does every device needs an ip address?
Devices needs an ip address to identify itself in network, so that data can be send or deliver to the particular IP address.
#what is difference between public ip and private ip?

#can two devices have the same private IP if they are on different network?
#which device decides where packets go on the internet?
why is the CIA traide important?
As an application security tester, why do you need to undersdatnd networking?
