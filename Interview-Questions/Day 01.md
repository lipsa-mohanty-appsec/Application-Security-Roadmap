# Day 1 Interview Questions:-
##why is a switch more secutre than hub?
  Answer:A hub boradcasts incoming data to every connected devices, making traffic easier to intercept.Av switch forwads frames only to designated mac address, reducing un necessary exposure of network traffic.
##why does google uses DNS?
  Answer:DNS translates a human readable domain name in to an IP address so the browser knows which server to connect.
##what happens if DNS stops working?
  Answer:If DNS stops working user will not requested data, no web page address will respond.
##why does every device needs an ip address?
  Answer:Devices needs an ip address to identify itself in network, so that data can be send,deliver or routed  to the particular IP address/ correct destination.
##what is difference between public ip and private ip?
  Answer:Public ip is accessible from the internet, assigned by ISP,globally unique, used for internet communication.
but Private ip is used inside private network, assigned by the router or network admin,can be reused in different private networks,it can be used inside a home or any business not open to use for anyone.,used for only local communication.
##can two devices have the same private IP if they are on different network?
  Answer:Yes, two different devices in different private network can have same ip address.
##which device decides where packets go on the internet?
  Answer:A router is the device decided where the pckage goes on internet.
##why is the CIA traide important?
  Answer:CIA Tride is important as it gives the foundtional framework that guides the organaization to secure their data, network and it system.
Confidentiality
Integrity
Availabaility
##As an application security tester, why do you need to undersdatnd networking?
  Answer:Major attacks happens via networks, to understand and to defend as a cyber security engineer i should understand networking.
